# PRUCOLY - Home Services Marketplace

A platform connecting clients with home service providers in Morocco.

## Setup
- Backend: `cd backend && npm install && npm run dev`
- Frontend: `cd frontend && npm install && npm run dev`

## Test accounts (after running `node seed.js`)
- Client: john@example.com / password123
- Provider: sarah@example.com / password123